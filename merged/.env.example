# Copy to .env.local (or set in your host's dashboard) before deploying.
#
# Public origin of the site, without a trailing slash. Used for canonical and
# Open Graph URLs, robots.txt and sitemap.xml. On Vercel the production domain
# is picked up automatically when this is left empty.
NEXT_PUBLIC_SITE_URL=
